#Function script 
#This function aims to simply take the square root of any given number
#The function can receive an input (numeric) and will simply print out its square root

SquareRoot<-function(x){
  y<-sqrt(x)
  
  print(y)
  
}

#test with:
SquareRoot(4)
